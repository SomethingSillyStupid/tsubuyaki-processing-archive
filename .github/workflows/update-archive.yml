name: Update archive placeholder

on:
  workflow_dispatch:
  schedule:
    - cron: '17 */4 * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase 1 placeholder
        run: |
          echo "Phase 2 will fetch #つぶやきProcessing posts using X_BEARER_TOKEN."
          test -f site/data/sketches.json
